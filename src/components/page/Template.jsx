/**
 * Template page
 * @param {boolean} mobileMode whether to use mobile or desktop view
 * @param {boolean} darkMode whether to use dark or light mode
 * @returns template page
 */
export const Template = ({ mobileMode, darkMode }) => {
  return (
    <div>
      TEMPLATE
    </div>
  );
}